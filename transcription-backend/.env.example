# OpenAI API Key (required for /summarize and /generate-quiz endpoints)
OPENAI_API_KEY=sk-...

# Whisper model to load (options: tiny, base, small, medium, large)
# Recommended for Railway starter tier (512MB-1GB RAM): base or tiny
# Recommended for 4GB+ RAM instances: medium
WHISPER_MODEL=base
